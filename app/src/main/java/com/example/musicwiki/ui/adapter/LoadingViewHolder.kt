package com.example.musicwiki.ui.adapter

import androidx.databinding.ViewDataBinding
import com.example.musicwiki.BaseClickListener
import com.example.musicwiki.BaseViewHolder

class LoadingViewHolder(view: ViewDataBinding) : BaseViewHolder<Any,BaseClickListener>(view) {
    override fun bind(item: Any, listener: BaseClickListener?) {
    }
}